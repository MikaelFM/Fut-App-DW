<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FutApp - Home</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@100..900&display=swap" rel="stylesheet">
    <link href="https://cdn.tailwindcss.com" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<%- include('includes/menus', { page: 'myevents' }) %>
<body class="min-h-screen bg-gradient-to-br bg-gray-950 p-4">
<div class="py-4 sm:ml-64">
    <div class="py-5 px-2 md:px-10">
        <div class="max-w-4xl mx-auto">
            <div class="bg-gray-900 rounded-xl shadow-xl py-8 bg-gray-950">
                <div class="text-center mb-8">
                    <h1 class="text-3xl font-bold text-white mb-2">Placar da Partida</h1>
                </div>
                <form method="post" action="/event/finalize">
                    <input type="hidden" name="event_id" value="<%= fields.id %>">
                    <div class="space-y-6">
                        <div class="bg-gray-950 rounded-lg p-6 border border-gray-700">
                            <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
                                <div class="bg-gray-900 rounded-lg p-4">
                                    <h3 class="font-semibold text-blue-400 mb-3 text-center">Time 1</h3>
                                    <div class="text-center">
                                        <input required name="gols_time_1" type="text" min="0" max="99" class="w-20 h-16 text-center text-3xl font-bold bg-gray-800 rounded-lg text-blue-400 focus:ring-2 focus:ring-blue-500" placeholder="0"/>
                                    </div>
                                    <div class="mt-5">
                                    <% if (fields.confirmados.filter((u) => u.team == 1).length === 0){ %>
                                        <div class="text-gray-500 text-sm text-center py-4">Nenhum jogador selecionado</div>
                                    <% } %>
                                    <% for (let user of fields.confirmados.filter((u) => u.team == 1)) { %>
                                        <div class="mb-2 bg-gray-800 p-3 rounded flex items-center justify-between border-l-4 border-blue-500">
                                            <span class="text-white text-sm font-medium"><%= user.user_name %></span>
                                        </div>
                                    <% } %>
                                    </div>
                                </div>

                                <div class="bg-gray-900 rounded-lg p-4">
                                    <h3 class="font-semibold text-red-400 mb-3 text-center">Time 2</h3>
                                    <div class="text-center">
                                        <input required name="gols_time_2" type="text" min="0" max="99" class="w-20 h-16 text-center text-3xl font-bold bg-gray-800 rounded-lg text-red-400 focus:ring-2 focus:ring-red-500" placeholder="0"/>
                                    </div>
                                    <div class="mt-5">
                                        <% if (fields.confirmados.filter((u) => u.team == 2).length === 0){ %>
                                            <div class="text-gray-500 text-sm text-center py-4">Nenhum jogador selecionado</div>
                                        <% } %>
                                        <% for (let user of fields.confirmados.filter((u) => u.team == 2)) { %>
                                            <div class="mb-2 bg-gray-800 p-3 rounded flex items-center justify-between border-l-4 border-blue-500">
                                                <span class="text-white text-sm font-medium"><%= user.user_name %></span>
                                            </div>
                                        <% } %>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <div class="flex justify-end">
                            <button class="px-8 py-3 bg-indigo-500 text-white rounded-lg hover:bg-indigo-700 font-semibold text-lg shadow-lg">
                                Salvar
                            </button>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
</body>
<script src="/js/tailwindConfig.js"></script>
</html>